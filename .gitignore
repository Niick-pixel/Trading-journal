# Your journal. Never commit trades or screenshots.
/data/

node_modules/
release/
.next/
out/
# Generic build output. Icon assets deliberately live in assets/ instead, so
# this ignore cannot swallow them the way it silently did once already.
build/
next-env.d.ts
*.tsbuildinfo

.DS_Store
.env*.local
.claude/

# Next.js writes these on boot; next.config.ts disables them
AGENTS.md
CLAUDE.md
